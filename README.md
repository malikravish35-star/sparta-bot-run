# SPARTA-BOT (runtime)

Telegram link-to-file bot. **Is repo me koi secret nahi hai** — saari config
environment variables se aati hai (BOT_TOKEN, API_ID, API_HASH, SESSION_STRING,
CACHE_CHANNEL, ADMIN_ID, etc.).

Deploy: Docker (`Dockerfile`) — Railway / Koyeb / Fly / kisi bhi host pe chalega.
